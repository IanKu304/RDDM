請使用anaconda prompt 執行以下指令

# 1. 切到工作目錄
# cd /d D:\Ian\NCKU\master2\Image_detection\Final_Report\RDDM


# 2. 用建立環境
conda create -n rddm python=3.10 -y
#conda activate base
#conda env create -f install_win.yaml

# 3. 啟用環境
conda activate rddm

# 4. 下載套件 
python -m pip install -U pip setuptools wheel

# 5. 安裝 PyTorch
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
## 檢查 GPU 是否可用
python -c "import torch; print('torch', torch.__version__); print('cuda', torch.cuda.is_available()); print('device', torch.cuda.get_device_name(0) if torch.cuda.is_available() else None)"


#  建議加的 PATH / 設定
git config --global core.longpaths true


#
pip install numpy pillow tqdm matplotlib einops opencv-python scipy scikit-image
pip install accelerate tensorboard
pip install lpips pytorch-fid
pip install colorama exceptiongroup


python -c "import accelerate; print('accelerate', accelerate.__version__)"






python gen_gtrain_flists.py ^
  --data_root "D:\Ianku\RDDM\data" ^
  --out_dir ".\data_gtrain" ^
  --train_split GT-RAIN_train ^
  --test_split GT-RAIN_val

python train_ablation.py --amp --beta_schedule linear --beta_end 0.02 --beta_scale 1.0


python eval_metrics.py ^
  --gt_flist ".\data_gtrain\test_gt.flist" ^
  --pred_dir ".\results\GT-RAIN__img256__bs1__acc2__steps120000__schedlinear__betaEnd0.02__betaScale1__seed10\test_timestep_10\ckpt_120" ^
  --match basename --resize pred_to_gt ^
  --lpips --fid --device cuda ^
  --out_dir ".\results\GT-RAIN__img256__bs1__acc2__steps120000__schedlinear__betaEnd0.02__betaScale1__seed10\metrics"

